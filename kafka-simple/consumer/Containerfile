FROM python:3.12-alpine

RUN apk add --no-cache tini

WORKDIR /app

COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY consumer.py index.html ./

EXPOSE 8000

ENTRYPOINT ["tini", "--"]
CMD ["python", "-u", "consumer.py"]
